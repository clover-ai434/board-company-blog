# 戦略メモ 2026-08-02

## 気づいたこと・懸念点

1. **業界ウォッチ(月水金)が7/31(金)に欠落していたことを確認した**: `posts/`のタイトルを`scripts/build.mjs`の`categoryOf`と同じ基準(「ウォッチ」または「【」を含むか)で洗い出すと、直近の業界ウォッチ記事は7/20・7/23・7/24・7/27・7/29の5本のみで、7/31(金)分が存在しない。7/31メモで「単に投稿前の可能性が高い」としていた懸念が、結果的に的中していたことになる。次の対象日は8/3(月)なので、そこで再開されるかを確認したい。単発の見落としか、月水金の運用自体が崩れつつあるのかは、このリポジトリだけでは判別できない。

2. **CTA導線の切り替え提案が7/30から数えて5日連続(7/30・7/31・8/1・8/2)で未反映**: `scripts/build.mjs`365行目付近の記事末尾CTAは、依然として`quiz.html`(汎用のAI活用度診断)を`boundary-check.html`(本命事業=オーバーサイトキットに直結する権限境界チェック)より先に案内している。同じ指摘を4回連続でメモに書いても状況が変わらないため、今回で指摘は最後にし、下の提案では「判断待ち」であることを明示的に確認する形に切り替える。

3. **新しい気づき: カテゴリ分類が「失敗実況」シリーズを実質的に拾えていない**: `archive.html`のカテゴリ集計を見ると「教訓・注意点 1件」「AI活用Tips 13件」「業界ウォッチ 5件」となっている。`categoryOf`関数(`scripts/build.mjs`262〜266行目)はタイトルに「落とし穴|失敗|注意|リスク」を含むかどうかだけで判定しており、実際には失敗談として書かれている「会社名を1週間で4回改名した話」(7/27)「CTAリンクが3日間放置されていた話」(7/28)「定期投稿タスクが4つ静かに止まった話」(7/29)は、タイトルにこれらの語が入っていないため軒並み「AI活用Tips」に分類されてしまっている。「教訓・注意点」1件は8/2の記事(タイトルに「失敗」を含む)だけで、8/1メモで提案した「失敗実況をまとめて読める入口」は、現状のカテゴリフィルタでは実現できていないことが分かった。

## 新しい提案

1. **業界ウォッチの投稿有無をビルド時にチェックする仕組みを追加する(前回までの提案の具体化・優先度引き上げ)**
   理由: 今回、実際に7/31の業界ウォッチが欠落していたことが確認できた。`scripts/build.mjs`のビルド処理に、直近の月・水・金それぞれについて`posts/`内に該当曜日の業界ウォッチ記事(カテゴリ判定で`業界ウォッチ`になるもの)が存在するかを突き合わせ、なければ標準出力に警告を出す、という数行程度の追加で実装できる。実際に見落としが起きた今、コストの低い改善として着手する価値があると考える。ただし今回はメモとしての提案に留め、実装はオーナーの了承を得てから行いたい(ビルドスクリプトの挙動変更のため)。

2. **カテゴリ判定にfrontmatterでの明示指定を許容する(または「失敗実況」を独立カテゴリにする)**
   理由: タイトルのキーワードだけに頼る現在の`categoryOf`は、実質的に内容と乖離した分類になっている。`npm run new-post`のfrontmatterに`category: 失敗実況`のような任意フィールドを追加できるようにし、指定があればそれを優先、なければ従来通りタイトルから推定する形にすれば、8/1メモで提案した「失敗談だけ読める入口」を`search.html?category=失敗実況`で実現できる。実装自体は小規模だが、frontmatter・build.mjs・search.htmlの3箇所に影響するため、方向性の了承を先に得たい。

3. **CTA導線の件は、今回で提案自体は打ち止めにし、次回以降は「進捗確認」のみに切り替える**
   理由: 同じ提案を5日連続で書いても状況が動いていない以上、これ以上繰り返すのは記録としての価値が薄い。オーナー側で優先度が低いと判断している可能性もあるため、今回を最後に「確認したいこと」で一度だけ意思確認し、返答があるまではこの件を毎回のメモに書くのをやめる(必要ならオーナー側から指示があった際に再開する)。

## オーナーに確認したいこと

- CTA導線切り替え(`quiz.html`→`boundary-check.html`優先)の件、進めてよいか・保留のままでよいか・別の方針があるか、一度だけ明確な返答がほしい。返答があるまでこの件の再提案は止める。
- 8/3(月)の業界ウォッチ投稿が再開されるか、また7/31の欠落が意図的な調整だったのか単なる見落としだったのか、分かれば教えてほしい。
- 「業界ウォッチ欠落チェック」「カテゴリのfrontmatter対応」の2つの実装提案について、進めてよいものがあれば指示がほしい(このリポジトリのAI CEOはビルドスクリプトの変更を独断で行わない方針としている)。
- 有料会員・noteフォロワーの現在の実数(このリポジトリからは今回も確認できなかった)。
