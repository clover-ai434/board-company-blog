# 戦略メモ 2026-08-24

## 気づいたこと・懸念点

1. **「フッターCTA修正」が4日間(8/20〜8/23メモ)止まっていた本当の原因が判明した。文言修正ではなく、修正が生成物にしか適用されていなかったことが原因。**
   8/21の`fa37825`コミットで、トップページ(`docs/index.html`)・検索ページ(`docs/search.html`)の「メンバーシップCTA」文言から、2026-08-11のピボットで実質終了した旧事業「オーバーサイトキット」への古い誘導(「権限設計テンプレート集を継続的に追加しています」)は一度削除されていた。しかし、この2ファイルは`npm run build`のたびに`scripts/build.mjs`内の`membershipBox`テンプレート文字列から**自動再生成される**もので、そのテンプレート自体は直っていなかった。そのため、8/22・8/23と記事投稿(=ビルド)が走るたびに、修正が静かに巻き戻り続けていた。加えて、記事本文末尾の`cta-box`(全記事に表示される`article-footer`)にも同じ「オーバーサイトキット」文言が残ったまま一度も修正されていなかった。
   **今回、`scripts/build.mjs`の`membershipBox`と`cta-box`のテンプレート文字列そのものを修正し、`npm run build`で全42記事+index.html+search.htmlを再生成した。** これで今後の自動ビルドでも巻き戻らない(ソース側を直したため)。文言は8/21にトップページ・検索ページで使われていたのと同じ表現(「ココナラで実際に売れた・売れなかった商品の舞台裏も公開しています」)に統一した。
   実装の位置づけ: 8/23メモで「小さく・元に戻せる文言修正であることを条件に、次回稼働時は自分の判断で実装を検討する」としていた条件に合致すると判断し、今回実施した。生成物への直接編集ではなくテンプレート(ソース)側の修正であり、`git revert`で即座に戻せる。
2. **`context/handover.md`が2026-07-24更新のまま**(8/21から継続して確認依頼中、未更新)。特に「オーバーサイトキット」を「本命の新規事業」と記載している部分は、8/11のピボット以降の実態(サービスA=Claude Code導入・権限設計支援、ココナラ商品群、note有料記事という現行の収益化構成)と食い違っている。オーバーサイトキット自体は無料テンプレートページ(`oversight-kit.html`)としては残っているが、有料の柱ではなくなっている模様。この食い違いに気づかず放置すると、クラウド側の自分が古い前提で提案を続けるリスクがあるため、繰り返し記載する。
3. Tips記事は8/10〜8/24まで15日連続で「〇〇をAIに任せたら、△△が減った」という同じ型が続いている。ネタの幅は保てているが、型の摩耗リスクが出てくる時期だと考える(気づきのみ、対応は提案2で)。

## 新しい提案

1. **`context/handover.md`の「今動いている事業」節を、8/11ピボット後の実態(サービスA中心+ココナラ商品群+note有料記事、オーバーサイトキットは無料テンプレートページとしての位置づけ)に合わせて書き換えることを提案する**(3回目の提案、内容を具体化)。
   理由: 単なる更新依頼ではなく、今回`scripts/build.mjs`の食い違いを実際に修正する過程で、食い違いの範囲がテンプレート文言だけでなく事業の位置づけの説明そのものにまで及んでいることが分かったため。次回以降のクラウド側の提案精度に直結する。
2. **Tips記事の型を月1回程度、「〇〇をAIに任せたら、△△が減った」以外の形式(例: 失敗談・比較・Q&A形式)に変えることを提案する**。
   理由: 15日連続で同じ文型が続いており、読者・検索エンジン双方に対して単調さが目立ち始める時期だと考えるため。ただし内容(業務効率化の実体験ベース)自体は今の型で機能しているので、型を大きく変えるのではなく散発的に変化をつける程度を想定。
3. 次回Tips記事のテーマとして「社内問い合わせのFAQ化」は8/24付で使用済みのため、次点として`tips-theme-log.md`の未着手領域から選定する(具体テーマは次回稼働時に確認)。

## オーナーに確認したいこと

- **`context/handover.md`の更新(提案1)を実施してよいか**。8/21から継続確認。
- 今回実施したフッターCTA修正(気づき1)の内容が実態と合っているか確認いただきたい。「ココナラで実際に売れた・売れなかった商品の舞台裏」という表現で問題なければ、そのまま運用を続ける。
- (継続確認・急ぎではない)note有料会員数・フォロワー数、サービスA・ココナラ商品群の閲覧・購入実績の現状。
